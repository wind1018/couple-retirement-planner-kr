<Window x:Class="NpsSimulator.Desktop.PasswordPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="암호화 비밀번호" Height="210" Width="420"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="PromptText" TextWrapping="Wrap" FontSize="14" Margin="0,0,0,10" />
        <PasswordBox x:Name="PasswordInput" Grid.Row="1" Padding="8" MinHeight="34" />
        <TextBlock Grid.Row="2" Text="8자 이상 입력하세요. 비밀번호를 잊으면 복구할 수 없습니다."
                   Foreground="#6B7280" Margin="0,8,0,0" />
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button Content="취소" IsCancel="True" />
            <Button Content="확인" IsDefault="True" Click="Confirm_Click" Background="{StaticResource PrimaryBrush}" Foreground="White" />
        </StackPanel>
    </Grid>
</Window>
